pacakge math

import simpleIO.*;

public class CheatForMath {
    public static void main(String[] args) {
          double a;
          double b;
          double c;
          double x1;
          double x2;
          double discriminant;

          a = dialog.readDouble("what is a value:");
          b = dialog.readDouble("what is b value:");
          c = dialog.readDouble("what is c value:");

          discriminant = b*b - 4ac 

          if (discriminant < 0){
            Console.print("no roots");
          }else if (discriminant == 0){
            x1 = -b + //figure out what a quare root is in simpleIO
            Console.print(x1 +" is your one real root");
          }else {
            x1 = -b +
            x2 = -b -
            Console.print(x1 + " is your root and " + x2 + " is another");
